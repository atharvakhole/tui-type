// Application State
pub mod app;

// Handles the events thread
pub mod event;

// Handles the events and updates the application state
pub mod handler;

// Initalises and exits the terminal
pub mod tui;

// Renders the UI
pub mod ui;
